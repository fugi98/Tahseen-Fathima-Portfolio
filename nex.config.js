/** @type {import('next').NextConfig} */
const nextConfig = {

  experimental: {
    appDir: true, // Ensure the app directory feature is enabled
  },
 // compiler: {
  //  styledComponents: true,
 // },
};

module.exports = nextConfig;
